import React from 'react';
import Test from '../Test/Test';

const Menubar = (Props) => {
    console.log(Props.country);
    return (
        <div>
            <h1 className='bg-primary'>kola</h1>
             
        </div>
    );
};

export default Menubar;